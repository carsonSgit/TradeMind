name: Convert README to HTML

on:
  push:
    paths:
      - 'README.md'
  workflow_dispatch:
  
jobs:
  convert-markdown:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Install Pandoc
        run: sudo apt-get install -y pandoc
      
      - name: Convert Markdown to HTML
        run: pandoc README.md -o design_doc/design_doc.html --standalone -c design_doc.css
      
      - name: Commit and push if design_doc.html changed
        env:
          MY_TOKEN: ${{ secrets.MK_TO_HTML_TOKEN }}
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add design_doc/design_doc.html
          git commit -m "Update design_doc.html" || exit 0
          git push --set-upstream https://${MY_TOKEN}@github.com/${GITHUB_REPOSITORY}.git
